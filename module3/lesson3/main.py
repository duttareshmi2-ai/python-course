# Arguments: They are given at the time of function calling.
# Positional Arguments: Are those arguments which are passed into a function and it gets matched to a parameter based upon the order they are written.
# Example:
# def subtract(a,b):
#     return a-b
# print(subtract(5.14,10.28))
# Recursion: When a function calls itself inside it's own body repeating the task on a smaller piece of problem each time.
# Example:
# def factorial(x):
#     if x==0 or x==1:
#         return 1
#     else:
#         return x*factorial(x-1)
# print(factorial(3))
#Classroom activities
# Tip, the waiter
# Outline:
# Let's create a function total_calc() that helps us calculate and print out the total amount paid at a restaurant. Given a bill amount and the percentage of the bill amount you decide to pay us a tip (tip_perc ), this function calculates the total amount you should pay.
# Cube of the cube
# Outline:
# Define a function to find a cube and define another function which let execute the cube function if the number is divisible by 3
# Factorial
# Outline:
# Write a program to find the factorial using recursive function.